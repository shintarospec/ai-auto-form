# AI AutoForm - ワークフロー・操作手順

## 目次

1. [管理者のワークフロー](#管理者のワークフロー)
2. [作業者のワークフロー](#作業者のワークフロー)
3. [システム全体のフロー](#システム全体のフロー)
4. [トラブルシューティング](#トラブルシューティング)

---

## 管理者のワークフロー

### 1. ターゲットリストの作成

営業先企業リストを作成します。

#### オプションA: CSVアップロード（一括登録）

**ステップ:**

1. 管理者画面を開く → [ターゲットリストタブ]
2. "新しいリストを作成" ボタンをクリック
3. リスト名を入力（例: "2025年1月営業先リスト"）
4. CSVファイルを選択してアップロード

**CSVフォーマット:**
```csv
企業名,URL,業界
株式会社ABC,https://abc.co.jp/contact,IT
株式会社XYZ,https://xyz.com/inquiry,製造業
```

**注意点:**
- ヘッダー行は必須
- URLは問い合わせフォームの直接URLが望ましい
- 文字コード: UTF-8

#### オプションB: 手動追加（個別登録）

**ステップ:**

1. リスト作成後、リストを選択
2. "企業を追加" ボタンをクリック
3. フォームに入力:
   - 企業名
   - URL
   - 業界（オプション）
4. "追加" ボタンで保存

---

### 2. 商品の登録

営業する商品・サービスを登録します。

**ステップ:**

1. [商品タブ] を開く
2. "新しい商品を追加" ボタンをクリック
3. フォームに入力:
   - 商品名（例: "AIチャットボット導入パッケージ"）
   - 価格（例: 500000）
   - 説明（例: "導入から運用まで完全サポート"）
4. "追加" ボタンで保存

**Tips:**
- 説明文はAIメッセージ生成時に活用される（今後実装）
- 価格は整数で入力（円単位）

---

### 3. 作業者の登録

フォーム送信を担当する作業者を登録します。

**ステップ:**

1. [作業者タブ] を開く
2. "新しい作業者を追加" ボタンをクリック
3. フォームに入力:
   - 名前
   - メールアドレス
   - スキルレベル（beginner / intermediate / advanced）
4. "追加" ボタンで保存

**スキルレベルガイド:**
- **beginner**: 1日10件目標
- **intermediate**: 1日20件目標
- **advanced**: 1日30件以上

---

### 4. プロジェクトの作成

ターゲットリスト、商品、作業者を組み合わせてプロジェクトを作成します。

**ステップ:**

1. [プロジェクトタブ] を開く
2. "新しいプロジェクトを作成" ボタンをクリック
3. フォームに入力:
   - プロジェクト名（例: "2025年1月営業キャンペーン"）
   - ターゲットリストを選択
   - 商品を選択
   - 作業者を選択（複数選択可能）
4. "作成" ボタンでプロジェクト開始

**自動処理:**
- プロジェクト作成時、選択したターゲットリストの全企業分のタスクが自動生成される
- タスクは選択した作業者にラウンドロビン方式で割り当てられる

**例:**
- ターゲットリスト: 100社
- 作業者: 3名（田中、佐藤、鈴木）
- 結果: 田中34件、佐藤33件、鈴木33件のタスクが自動割り当て

---

### 5. 進捗の確認

プロジェクトの進行状況を確認します。

**現在の確認方法:**

1. [プロジェクトタブ] で各プロジェクトを選択
2. タスク数カウントを確認

**今後実装予定:**
- リアルタイム進捗グラフ
- 作業者別パフォーマンス
- 送信成功率の可視化

---

## 作業者のワークフロー

### 基本フロー

```
1. ログイン
   ↓
2. タスク一覧を確認
   ↓
3. タスクを選択
   ↓
4. 「自動送信スタート」ボタンをクリック
   ↓
5. ブラウザが自動起動、フォーム入力
   ↓
6. reCAPTCHAをクリック（手動）
   ↓
7. 入力内容を確認
   ↓
8. 送信ボタンをクリック（手動）
   ↓
9. 送信完了を確認
   ↓
10. ポイント獲得（+50pt）
   ↓
11. 次のタスクへ（3秒後に自動進行）
```

---

### 詳細手順

#### ステップ1: ログイン

**現在（LocalStorage版）:**
- URLを直接開く: `http://localhost:8000/worker-console.html`
- 作業者情報が自動的にLocalStorageから読み込まれる

**将来（認証実装後）:**
- メールアドレス + パスワード入力
- JWTトークン発行

---

#### ステップ2: タスク一覧の確認

**画面要素:**
- 自分に割り当てられたタスクが一覧表示される
- フィルター機能:
  - 全て
  - 未着手（pending）
  - 作業中（in_progress）
  - 完了（completed）

**タスクカードの情報:**
- 企業名
- 企業URL
- ステータスバッジ
- アクションボタン

---

#### ステップ3: タスクの選択

1. タスクカードの「開始」ボタンをクリック
2. 詳細モーダルが開く

**モーダル内容:**
- **作業の流れ** セクション（青いグラデーション背景）
  1. 自動送信ボタンをクリック
  2. ブラウザが起動して自動で入力される
  3. reCAPTCHAをクリックして送信ボタンを押す
- **CTAメッセージ**（黄色背景）
  - "流れを確認したらボタンをクリック！"
- **アクションボタン**
  - 🚀 自動送信スタート（メインボタン・青色）
  - NG（赤色・企業が適切でない場合）
  - スキップ（グレー・一時保留）

---

#### ステップ4: 自動送信スタート

**「🚀 自動送信スタート」をクリック時の動作:**

1. ブラウザが自動起動（Webkit/Chromium）
2. 指定URLへ自動遷移
3. フォーム検出
4. 以下のフィールドに自動入力:
   - 会社名（company）
   - 担当者名（name）
   - メールアドレス（email）
   - 電話番号（phone）
   - メッセージ（message）

**所要時間:** 約2秒

---

#### ステップ5-8: 手動操作（Human-in-the-Loop）

システムは**60秒間待機**します。この間に以下を手動で行います:

**手動操作が必要な理由:**
- reCAPTCHAの突破（自動化は規約違反）
- 入力内容の最終確認
- フォーム固有の特殊フィールド対応

**手動操作の流れ:**

1. **reCAPTCHAをクリック**
   - "私はロボットではありません" にチェック
   - 画像選択が出たら指示に従う

2. **入力内容の確認**
   - 会社名が正しいか
   - メッセージが適切か
   - 必須フィールドが埋まっているか

3. **送信ボタンをクリック**
   - フォームの送信ボタンを押す

**注意事項:**
- 60秒以内に送信を完了させること
- ブラウザを閉じると処理が中断される

---

#### ステップ9: 送信完了の自動検出

システムは以下の3つの方法で送信完了を検出します:

1. **URL変化検出**
   - サンキューページへの遷移を検出
   - 例: `/contact` → `/thank-you`

2. **成功メッセージ検出**
   - "送信完了しました" などのメッセージを検出
   - 可視性チェック付き（非表示要素は無視）

3. **フォームリセット検出**
   - フォームの入力値がクリアされたかチェック
   - 最も確実な方法

**検出結果:**
- ✅ **送信完了** → `submitted: true`
- ⚠️ **入力のみ** → `submitted: false`

---

#### ステップ10: ポイント獲得

**送信完了時（submitted: true）:**
- ✅ 緑色の成功メッセージ表示
- 🎉 +50ポイント獲得
- ステータスが「完了」に変更
- 3秒後に次のタスクへ自動進行

**入力のみ（submitted: false）:**
- ⚠️ 黄色の警告メッセージ表示
- ポイントは加算されない
- リトライボタンが表示される
- ステータスは「作業中」のまま

---

#### エッジケース対応

**ケース1: ブラウザを手動で閉じた場合**
- システムが即座に検出
- タスクステータスは変更されない
- リトライ可能

**ケース2: 60秒経過した場合**
- 自動的に処理終了
- `submitted: false` として記録
- リトライ可能

**ケース3: フォームが見つからない場合**
- エラーメッセージ表示
- 管理者に報告（NGボタン推奨）

---

### NGボタンの使い方

企業が不適切な場合に使用します。

**使用すべきケース:**
- 企業が既に廃業している
- URLが404エラー
- 問い合わせフォームが存在しない
- BtoC企業（BtoB商材の場合）

**クリック時の動作:**
- タスクステータスが「NG」に変更
- 管理者に通知（今後実装）
- ポイントは加算されない
- 次のタスクへ

---

### スキップボタンの使い方

一時的に後回しにしたい場合に使用します。

**使用すべきケース:**
- フォームが複雑で時間がかかりそう
- 今は集中できない
- 他のタスクを優先したい

**クリック時の動作:**
- タスクステータスは変更されない
- モーダルが閉じる
- 後でまた取り組める

---

## システム全体のフロー

### データフロー図

```
管理者画面
  │
  ├─ ターゲットリスト作成
  │   ├─ CSV Upload
  │   └─ Manual Entry
  │        ↓
  │   LocalStorage
  │   (targets, targetLists)
  │
  ├─ 商品登録
  │        ↓
  │   LocalStorage
  │   (products)
  │
  ├─ 作業者登録
  │        ↓
  │   LocalStorage
  │   (workers)
  │
  └─ プロジェクト作成
       ├─ Select: TargetList
       ├─ Select: Product
       └─ Select: Workers
            ↓
       Auto Generate Tasks
            ↓
       LocalStorage
       (projects, tasks)
            ↓
       Round-Robin Assignment
            ↓
       作業者に通知（今後実装）

作業者画面
  │
  ├─ タスク一覧取得
  │   ← LocalStorage (tasks)
  │
  ├─ タスク選択
  │
  └─ 自動送信スタート
       ↓
   POST /api/tasks/{id}/submit
       ↓
   Flask API
       ↓
   Playwright Service
       ├─ Launch Browser (Webkit/Chromium)
       ├─ Navigate to URL
       ├─ Fill Form (5 fields)
       ├─ Wait 60s (Human-in-the-Loop)
       ├─ Detect Submission
       └─ Close Browser
       ↓
   Return Result
   {
     submitted: true/false,
     fields_filled: [...],
     message: "..."
   }
       ↓
   作業者画面
       ├─ submitted: true
       │   ├─ Update Status: completed
       │   ├─ Add Points: +50
       │   └─ Auto Advance (3s)
       │
       └─ submitted: false
           ├─ Keep Status: in_progress
           ├─ No Points
           └─ Show Retry Button
```

---

### タスクライフサイクル

```
[pending] 未着手
   ↓ 作業者が「開始」をクリック
[in_progress] 作業中
   ↓ 自動送信スタート
[processing] 処理中（ブラウザ起動中）
   ↓ 送信完了検出
   ├─ submitted: true
   │   ↓
   │ [completed] 完了（+50pt）
   │
   └─ submitted: false
       ↓
     [in_progress] 作業中（リトライ可能）

特殊ケース:
[ng] NG（企業が不適切）
[skipped] スキップ（一時保留）
```

---

## トラブルシューティング

### 問題1: ブラウザが起動しない

**症状:**
- 「自動送信スタート」をクリックしても何も起こらない

**原因候補:**
- Playwrightが未インストール
- ブラウザバイナリが不足

**解決方法:**

```bash
# Playwrightのインストール確認
pip list | grep playwright

# ブラウザのインストール
playwright install webkit chromium

# システム依存関係のインストール
playwright install-deps
```

---

### 問題2: Chromiumがクラッシュする（macOS Apple Silicon）

**症状:**
- ブラウザ起動時に即座にクラッシュ
- エラー: `SEGV_ACCERR`

**原因:**
- Chromiumが Apple Silicon に完全対応していない

**解決方法:**
- Webkitを使用（自動フォールバック済み）
- または Rosetta 2 経由で実行

```bash
# Rosetta 2経由（非推奨・遅い）
arch -x86_64 python backend/app.py
```

**推奨:** システムが自動的にWebkitにフォールバックするため対応不要

---

### 問題3: ポート5000が使えない（macOS）

**症状:**
- Flask起動時にエラー
- `OSError: [Errno 48] Address already in use`

**原因:**
- macOS AirPlayがポート5000を使用

**解決方法:**

```bash
# PORT環境変数を設定
export PORT=5001
python backend/app.py
```

または

```bash
# AirPlayを無効化
システム設定 → 一般 → AirPlayレシーバー → OFF
```

---

### 問題4: 送信完了が検出されない

**症状:**
- 送信ボタンを押したのに `submitted: false` になる

**原因候補:**
1. URL変化がない
2. 成功メッセージが非表示
3. フォームがリセットされない

**解決方法:**

**パターンA: サンキューページがある場合**
- 自動検出される（URL変化）

**パターンB: 同じページに成功メッセージ表示**
- 成功メッセージが可視であれば自動検出
- 非表示の場合、フォームリセット検出に頼る

**パターンC: フォームがリセットされる**
- 最も確実な方法
- 入力値がクリアされたら送信完了と判定

**デバッグ方法:**

管理者に以下を報告:
- フォームURL
- 送信後のURL
- 成功メッセージの有無
- フォームがクリアされるか

---

### 問題5: reCAPTCHAが検出されない

**症状:**
- reCAPTCHAがあるのに通知されない

**原因:**
- reCAPTCHA検出機能が未実装

**暫定対処:**
- 60秒の待機時間中に手動でクリック
- 将来的に通知機能を実装予定

---

### 問題6: LocalStorageデータが消えた

**症状:**
- 登録した作業者やプロジェクトが消えている

**原因:**
- ブラウザのキャッシュクリア
- シークレットモード使用

**解決方法:**

**予防策:**
```javascript
// バックアップ用スクリプト（開発者コンソールで実行）
const backup = {
  workers: localStorage.getItem('workers'),
  products: localStorage.getItem('products'),
  projects: localStorage.getItem('projects'),
  tasks: localStorage.getItem('tasks'),
  targets: localStorage.getItem('targets'),
  targetLists: localStorage.getItem('targetLists')
};
console.log(JSON.stringify(backup));
// 出力結果をテキストファイルに保存
```

**復元:**
```javascript
// バックアップから復元
const backup = { /* バックアップデータ */ };
Object.entries(backup).forEach(([key, value]) => {
  localStorage.setItem(key, value);
});
location.reload();
```

**根本的解決:**
- PostgreSQLへの移行（Phase 4で実装予定）

---

### 問題7: APIエラーが発生する

**症状:**
- `Failed to fetch` エラー
- CORS エラー

**原因候補:**
1. Flaskサーバーが起動していない
2. ポート番号が間違っている
3. CORS設定が不足

**解決方法:**

**1. サーバーの起動確認:**
```bash
# プロセス確認
ps aux | grep python

# ログ確認
tail -f backend/app.log
```

**2. ポート番号の確認:**
```javascript
// worker-console.html 内
const API_BASE_URL = 'http://localhost:5001/api';  // ←macOSは5001
```

**3. CORS設定確認:**
```python
# backend/app.py
CORS(app, resources={
    r"/api/*": {
        "origins": ["http://localhost:*", "https://*.app.github.dev"]
    }
})
```

---

### 問題8: タスクが割り当てられない

**症状:**
- プロジェクトを作成してもタスクが0件

**原因:**
- ターゲットリストが空
- JavaScriptエラー

**解決方法:**

**1. ターゲットリスト確認:**
```javascript
// 開発者コンソールで実行
const targetLists = JSON.parse(localStorage.getItem('targetLists'));
console.log(targetLists);

const targets = JSON.parse(localStorage.getItem('targets'));
console.log(targets);
```

**2. エラーログ確認:**
- ブラウザの開発者ツール（F12）→ Consoleタブ

**3. 手動タスク作成:**
```javascript
const tasks = JSON.parse(localStorage.getItem('tasks')) || [];
tasks.push({
  id: Date.now(),
  projectId: 1,
  workerId: 1,
  companyName: 'テスト株式会社',
  companyUrl: 'http://localhost:8000/test-form.html',
  status: 'pending',
  message: null
});
localStorage.setItem('tasks', JSON.stringify(tasks));
location.reload();
```

---

### サポート連絡先

**開発チーム:**
- GitHub Issues: [Repository URL]
- Slack: #ai-autoform-support
- Email: support@example.com

**報告時に含めるべき情報:**
1. 症状の詳細
2. エラーメッセージのスクリーンショット
3. ブラウザのコンソールログ
4. 環境情報（OS、ブラウザバージョン）
5. 再現手順
